apply plugin: "war"

bootWar {
    mainClass = "com.jhipster.ecommerce.invoice.InvoiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
